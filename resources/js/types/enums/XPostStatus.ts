/**
 * X Post status enum matching PHP XPostStatus enum.
 */
export enum XPostStatus {
    Draft = 'draft',
    Scheduled = 'scheduled',
    Published = 'published',
    Failed = 'failed',
    Cancelled = 'cancelled',
}
